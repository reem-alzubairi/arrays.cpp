# arrays.cpp
Arrays practice in C++
# Lesson 1: Arrays in C++

This is my first C++ practice lesson.

## arrays.cpp
- I made an array `nums` with 7 numbers.
- I used `sizeof` to check the size of the data type and the whole array.
- I calculated the number of elements automatically without writing the number manually.
- This lesson helped me practice the basics and understand how array sizes work.

## Notes
- You can change the numbers or try adding more arrays for practice.

## Lesson 2: nested_loops_for.cpp

This lesson is about my `nested_loops_for.cpp` practice in C++.

### What I did:
- I created an array `products` with 3 items and an array `sizes` with 3 sizes.
- I used a nested `for` loop: the outer loop for products, the inner loop for sizes.
- At first, when I printed sizes, each one appeared on a new line.
- Then I changed it so all sizes print in the same line.
- I made the last comma disappear after the last size.
- I added a separator line `=====================` after each product for clarity.

### What I learned:
- How to use nested loops with arrays.
- How to control output formatting.
- How to combine multiple arrays in loops for better presentation.
